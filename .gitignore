# OS
.DS_Store
Thumbs.db
desktop.ini

# Local environment files (CONTIENEN SECRETOS, NUNCA subir)
.env
.env.local
.env.*.local
.env.*.production
*.pem.local

# .env.example SÍ va al repo (es la plantilla pública)

# Archivos temporales de editores
*.swp
*.swo
*~
*.tmp
*.bak
*.orig
*.rej

# AI assistants y editores AI (cualquiera que uses en el futuro)
.claude/
.cursor/
.aider*
.continue/
.codeium/
.cody/
.cline/
.windsurf/
.roo/
.trae/
.kiro/
.augment/
.gemini/
.mcp.json
claude_desktop_config.json

# Otros editores / IDEs
.idea/
.vs/

# Node / SvelteKit
node_modules/
/build/
/.svelte-kit/
/package/
/dist/
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust / Tauri
src-tauri/target/
src-tauri/binaries/
src-tauri/gen/
src-tauri/check_output*.txt
src-tauri/check_final.txt

# Tauri Signer (NUNCA subir llaves)
/.tauri/
tauri.key
tauri.key.pub
*.pem

# Backups
_backup/
*.rar
*.zip
*.7z

# Dev debris (logs, scripts, outputs de debugging)
check_output.txt
tauri_stderr.txt
tauri_stdout.txt
dev_log.md
extract_arguments.py
inspect_regs.py
scratch/

# Documentación interna (no es para el repo público)
DB_EXPERIMENTAL.md
RELEASE_PROTOCOL.md

# Documentación personal de Luis (no para el repo)
docs/ANALYSIS.md

# Backups temporales de git (filter-repo, etc.)
.git.backup.*

# Archivos temporales de tooling (expressions, secrets, etc.)
expressions.txt
secrets-to-redact.txt
*.local.txt
